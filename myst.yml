# See docs at: https://mystmd.org/guide/frontmatter
version: 1
project:
  id: 545a40d2-49ae-44a1-84e8-0108865b0af1
  # title:
  # description:
  # keywords: []
  # authors: []
  github: https://github.com/PanxoPanza/metodos_numericos_mec301
  # To autogenerate a Table of Contents, run "jupyter book init --write-toc"
  # Jupyter Book 2 / MyST no numera capítulos ni secciones por defecto
  # (a diferencia de JB1). Esto lo restaura.
  numbering:
    headings: true
    title:
      enabled: true
      # offset 0 = numeración plana: los capítulos son 1, 2, 3...
      # en vez de heredar el nivel de anidación del _toc.yml (1.1, 1.2...)
      offset: 0
site:
  template: book-theme
  # options:
  #   favicon: favicon.ico
  #   logo: site_logo.png
